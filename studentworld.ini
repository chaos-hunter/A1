# WorldGen configuration file

# Use a fixed seed for deterministic tests
seed=1234

# Number of rooms to generate
num_rooms=10

# Overall map dimensions (used to size room grid cells)
map_width=80
map_height=40

# Room base size and variation
base_room_width=5
base_room_height=5
room_size_variance=0

# Max entities per room
max_monsters_per_room=2
max_items_per_room=2

# Spawn probabilities (percent)
monster_spawn_chance=100
item_spawn_chance=100